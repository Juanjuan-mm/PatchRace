# Private beta record format

This directory contains the reviewed schema and a non-counting template.
Actual participant records are private, local-sensitive, and belong only under
`.artifacts/private-beta/participants/`.

Never commit completed participant records, raw notes, reports, code, paths,
credentials, or contact information. `sample: true` records and placeholder
values are rejected by the BETA-02 gate.
